# Icons

Place `icon.ico` (256×256, multi-resolution) here before running `npm run dist`.
electron-builder will use it as the application and installer icon.

If you do not have a real icon, you can use a temporary one, but Windows will show a generic icon in the Start menu and installer.
